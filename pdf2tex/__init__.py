"""pdf2tex — rule-based PDF → LaTeX converter.

Phase 1 (skeleton): extract a text layer with PyMuPDF, assemble naive
paragraphs, and emit a complete, compilable ``.tex`` document.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
